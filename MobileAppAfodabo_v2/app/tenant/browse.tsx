/**
 * Tenant Browse Properties — reuses the existing guest browsing experience so
 * tenants browse the same public property catalogue while staying logged in.
 */

export { default } from "../guest/explore";
