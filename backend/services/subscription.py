# DEPRECATED: This is the old subscription service (uses plan_type + subscription table).
# Replaced by services/subscriptions.py (uses plan_id + subscription_plans table).
# Payment flows now use Pesapal via services/pesapal.py.
# Kept as reference only.
