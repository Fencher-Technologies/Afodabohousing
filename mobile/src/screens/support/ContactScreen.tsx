import { SupportInfoScreen } from './SupportInfoScreen';

export function ContactScreen() {
  return (
    <SupportInfoScreen
      body="For support, contact the Afodabo Housing team through the official support channels configured for the platform."
      eyebrow="Support"
      title="Contact Support"
    />
  );
}
